# Base URL of the MediTime API (apps/api). Use your machine's LAN IP (not
# "localhost") when testing on a physical device or Expo Go, e.g.
# http://192.168.1.23:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
